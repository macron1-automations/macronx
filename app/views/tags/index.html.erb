<div class="mb-6 flex items-start justify-between">
  <div>
    <h1 class="text-2xl font-semibold text-gray-900">Tags</h1>
    <p class="mt-1 text-sm text-gray-500">Manage tags for organizing inbox items</p>
  </div>
  <%= link_to new_tag_path, class: "inline-flex items-center gap-x-1.5 rounded-md bg-gray-900 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-gray-700 transition-colors duration-150" do %>
    <svg class="-ml-0.5 h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
      <path d="M10.75 4.75a.75.75 0 0 0-1.5 0v4.5h-4.5a.75.75 0 0 0 0 1.5h4.5v4.5a.75.75 0 0 0 1.5 0v-4.5h4.5a.75.75 0 0 0 0-1.5h-4.5v-4.5Z" />
    </svg>
    New Tag
  <% end %>
</div>

<% if @tags.any? %>
  <div class="overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wider text-gray-500">Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wider text-gray-500">Inbox Items</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-semibold uppercase tracking-wider text-gray-500">Created</th>
          <th scope="col" class="relative px-6 py-3"><span class="sr-only">Actions</span></th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100 bg-white">
        <% @tags.each do |tag| %>
          <tr class="hover:bg-gray-50 transition-colors duration-100">
            <td class="px-6 py-4 text-sm font-medium text-gray-900">
              <%= link_to tag.name, tag, class: "hover:text-indigo-600 transition-colors duration-150" %>
            </td>
            <td class="px-6 py-4 text-sm text-gray-500">
              <span class="inline-flex items-center rounded-full bg-indigo-50 px-2.5 py-0.5 text-xs font-medium text-indigo-700">
                <%= @tag_inbox_counts.fetch(tag.id, 0) %>
              </span>
            </td>
            <td class="px-6 py-4 text-sm text-gray-500 whitespace-nowrap"><%= tag.created_at.strftime('%b %d, %Y') %></td>
            <td class="px-6 py-4 text-right text-sm font-medium whitespace-nowrap">
              <div class="flex items-center justify-end gap-x-3">
                <%= link_to 'Edit', edit_tag_path(tag), class: "text-gray-600 hover:text-gray-900 transition-colors duration-150" %>
                <%= button_to 'Delete', tag, method: :delete,
                      data: { turbo_confirm: "Delete \"#{tag.name}\"? This cannot be undone." },
                      class: "text-red-600 hover:text-red-900 transition-colors duration-150 bg-transparent border-0 p-0 cursor-pointer font-medium" %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div class="flex flex-col items-center justify-center rounded-lg border-2 border-dashed border-gray-300 py-16 text-center">
    <svg class="mx-auto h-12 w-12 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" d="M9.568 3H5.25A2.25 2.25 0 0 0 3 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 0 0 5.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 0 0 9.568 3Z" />
      <path stroke-linecap="round" stroke-linejoin="round" d="M6 6h.008v.008H6V6Z" />
    </svg>
    <h3 class="mt-4 text-sm font-semibold text-gray-900">No tags</h3>
    <p class="mt-1 text-sm text-gray-500">Create a tag to start organizing inbox items.</p>
    <%= link_to 'New Tag', new_tag_path, class: "mt-6 inline-flex items-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 transition-colors duration-150" %>
  </div>
<% end %>
